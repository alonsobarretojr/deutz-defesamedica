# Preenchido pelo cliente antes do build de produção.
# Todas com NEXT_PUBLIC_ pois o site é 100% estático (sem servidor).

# JSON-LD (LegalService)
NEXT_PUBLIC_POSTAL_CODE=
NEXT_PUBLIC_AREA_SERVED=

# Tracking (deixar em branco desativa a tag correspondente)
NEXT_PUBLIC_GTM_ID=